{
  "name": "@rello-platform/nurture-goals",
  "version": "0.8.0",
  "description": "Canonical NurtureGoal registry + priming/topic-exclusion metadata + inferNurtureGoal lead-state inference for the Rello platform — consumed by Rello (campaigns + nurture-precedence-authority connector) and Milo Engine (goal-driven composition).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src", "README.md", "LICENSE"],
  "dependencies": {
    "@rello-platform/signals": "github:rello-platform/signals#v0.5.1"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test tests/*.test.mjs",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.4.0"
  },
  "repository": {
    "type": "git",
    "url": "github:rello-platform/nurture-goals"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "license": "UNLICENSED"
}
